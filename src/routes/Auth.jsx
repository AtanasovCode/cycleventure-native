import React, { useEffect } from "react";
import {
    View,
    Text,
    Image,
} from "react-native";
import { useStore } from "../../useStore";
import { supabase } from "../../supabase";

const Auth = () => {
    return (
        <View>
            <Text>Auth</Text>
        </View>
    );
}

export default Auth;